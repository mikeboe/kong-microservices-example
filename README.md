# Kong API GATEWAY with Microservices example

Kong API Gateway example with Node & GO Backend service, Authenticaiton, Rate Limiting, Rate Size Limiting Plugins.

## Run locally

```bash
# Run
docker compose up -d

# Stop
docker compose down
```
